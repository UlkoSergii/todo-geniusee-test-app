import { StyleSheet } from 'react-native';

import { SIZES } from '../../../../constants/sizes';

export default StyleSheet.create({
    listFooter: {
        height: SIZES.ADD_BUTTON.HEIGHT + SIZES.ADD_BUTTON.MARGIN_BOTTOM,
    },
});
